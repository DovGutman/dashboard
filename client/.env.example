# Vite exposes only variables prefixed with VITE_ to the browser.
# Point the client at the DashboardService base URL (optional).
# If omitted, the client uses same-origin for /api/*.
VITE_API_BASE_URL=http://localhost:5131

# SignalR tickets hub (optional)
VITE_TICKETS_HUB_PATH=/hubs/tickets
